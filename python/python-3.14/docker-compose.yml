services:
  python-app:
    image: python:3.14.3
    container_name: python-3.14.3-app
    volumes:
      - ./app:/app
    working_dir: /app
    command: python app.py
    ports:
      - "5000:5000"
    restart: no
