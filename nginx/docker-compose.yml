services:
  nginx:
    image: nginx:1.29.4-alpine3.23
    container_name: nginx-reverse-proxy
    restart: no
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    networks:
      - nginx-network

networks:
  nginx-network:
    driver: bridge

